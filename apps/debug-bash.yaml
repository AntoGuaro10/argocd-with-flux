apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: debug-bash
  namespace: argocd
spec:
  project: default
  source:
    repoURL: 'https://github.com/AntoGuaro10/debug-bash.git'
    targetRevision: HEAD
    path: k8s
  destination:
    server: https://kubernetes.default.svc
    namespace: debug
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
    - CreateNamespace=true
